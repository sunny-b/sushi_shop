<ul id="items">
  {{#each menu_items}}
  <li data-id="{{id}}">
    <article>
      <header>
        <figure>
          <img src="{{cover}}" alt="menu-item">
        </figure>
        <h2 class="name">{{name}}</h2>
      </header>
      <p class="price">${{format_price price}}</p>
      <footer>
        <a href="#" class="add_cart">Add to cart</a>
      </footer>
    </article>
  </li>
  {{/each}}
</ul>